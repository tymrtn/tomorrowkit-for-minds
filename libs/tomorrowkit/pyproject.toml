[project]
name = "tomorrowkit"
version = "0.2.0"
description = "Tomorrowkit orientation and provisional patent matter workspace for Minds"
readme = "README.md"
license = "LicenseRef-FCL-1.0-MIT"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "flask>=3.0",
    "flask-sock>=0.7",
    "imbue-common",
    "loguru>=0.7",
    "pydantic>=2.0",
    "werkzeug>=3.0",
]

[dependency-groups]
dev = [
    "import-linter>=2.0",
    "inline-snapshot>=0.20",
    "pyright>=1.1.400",
    "pytest>=8.0",
]

[project.scripts]
tomorrowkit = "tomorrowkit.runner:main"
tomorrowkit-workspace = "tomorrowkit.agent_tools:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tomorrowkit"]
