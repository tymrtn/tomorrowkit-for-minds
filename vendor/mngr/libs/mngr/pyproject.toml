[project]
name = "imbue-mngr"
version = "0.2.17"
description = "Library for managing AI coding agents across different hosts"
requires-python = ">=3.12"
license = "MIT"
readme = "README.md"
authors = [
    {name = "Imbue"},
    {name = "Josh Albrecht"},
    {name = "Evan Ryan Gunter"},
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
]
dependencies = [
    "imbue-common==0.1.20",
    "overlay==0.1.0",
    "resource-guards==0.1.8",
    "cel-python>=0.5.0",
    "click>=8.0",
    "click-option-group>=0.5.6",
    "concurrency-group==0.1.20",
    "coolname>=3.0.0",
    "cryptography>=42.0",
    # pinning docker because the guard registration module
    # (register_guards_docker.py) monkeypatches its internal methods for test
    # resource guards; new versions may move or rename them.
    "docker==7.1.0",
    "packaging>=21.0",
    "pydantic>=2.0",
    "psutil>=5.9",
    "setproctitle>=1.3",
    "python-dotenv>=1.0",
    "pyinfra>=3.0",
    "paramiko>=3.0",
    "pluggy>=1.5.0",
    "pygtail>=0.14.0",
    "markdown-it-py>=2.2.0",
    # requests is the transport docker-py is built on; we catch its connection
    # exceptions directly to tell "daemon unreachable" from "daemon errored".
    "requests>=2.26",
    "rich>=13.0",
    "tabulate>=0.9.0",
    "tenacity>=8.0",
    "tomlkit>=0.12.0",
    "urwid>=2.2.0",
]

[project.urls]
Homepage = "https://github.com/imbue-ai/mngr"
Repository = "https://github.com/imbue-ai/mngr"

[project.scripts]
mngr = "imbue.mngr.main:cli"

[project.entry-points.resource_guards]
mngr = "imbue.mngr.register_guards:register_mngr_guards"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["imbue"]
exclude = ["*_test.py", "test_*.py", "**/conftest.py", "**/testing.py", "**/utils/plugin_testing.py"]

# Ship the help-topic doc files inside the package so `mngr help <topic>` works
# in a PyPI wheel install (the top-level docs/ tree is otherwise not packaged).
# builtin_help_topics.py reads these from imbue/mngr/docs at runtime;
# test_builtin_topic_docs_are_force_included_in_wheel enforces that every
# built-in topic's doc lives under one of these dirs.
[tool.hatch.build.targets.wheel.force-include]
"docs/concepts" = "imbue/mngr/docs/concepts"
"docs/commands/generic" = "imbue/mngr/docs/commands/generic"

[tool.uv.sources]
imbue-common = { workspace = true }
concurrency-group = { workspace = true }
overlay = { workspace = true }
resource-guards = { workspace = true }
# skitwright is used by e2e tests (not a runtime dependency); resolved via workspace
# mngr_modal is used by e2e conftest (shared truncate_modal_name); resolved via workspace

# Shared pytest settings (markers, filterwarnings, coverage report config)
# are centralized in:
#   libs/imbue_common/imbue/imbue_common/conftest_hooks.py
# The settings below remain here because they are read from pyproject.toml
# before any hooks run: addopts, coverage.run, inline-snapshot, timeout_func_only.
[tool.pytest.ini_options]
timeout_func_only = true
# Use xunit1 format to enable record_xml_attribute fixture for consistent classnames
junit_family = "xunit1"
testpaths = ["."]
addopts = [
  "--cov=imbue.mngr",
  "--cov-report=term-missing",
  "--cov-report=xml",
  "--durations=20",
  "--timeout=10",
  "--timeout-method=signal",
  "--slow-tests-to-file",
  "--coverage-to-file",
]

[tool.coverage.run]
parallel = true
concurrency = ["multiprocessing"]
omit = [
  "*_test.py",
  "test_*.py",
  "*/tests/*",
  "*/conftest.py",
  "*/testing/*",
  "*/utils/testing.py",
  "*/utils/plugin_testing.py",
  "*/utils/mock_provider.py",
  "*/providers/docker/instance.py",
  "*/providers/docker/volume.py",
  "*/providers/docker/testing.py",
]

[tool.coverage.report]
fail_under = 85
omit = [
  "*_test.py",
  "test_*.py",
  "*/tests/*",
  "*/conftest.py",
  "*/testing/*",
  "*/utils/testing.py",
  "*/utils/plugin_testing.py",
  "*/utils/mock_provider.py",
  "*/providers/docker/instance.py",
  "*/providers/docker/volume.py",
  "*/providers/docker/testing.py",
  # Minds app has its own coverage configuration (apps/minds/pyproject.toml)
  "*/minds/*",
  # These packages are gated individually by their own (lower) fail_under
  # thresholds; excluding them from the combined report keeps the
  # combined 80% gate focused on the "main" set of packages and
  # prevents their lower coverage from dragging the combined mean down.
  "*/mngr_vps/*",
  "*/mngr_vultr/*",
]

[tool.inline-snapshot]
default-flags=["report"]
default-flags-tui=["report"]
default-flags-ide=["report"]
format-command="uv run ruff format --force-exclude --config pyproject.toml --stdin-filename {filename}"

[tool.pyright]
venvPath = "../.."
venv = ".venv"
pythonVersion = "3.12"
strict = ["**/*.py"]
