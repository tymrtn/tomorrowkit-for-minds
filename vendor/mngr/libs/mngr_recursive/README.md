# mngr-recursive

Recursive mngr plugin.

A plugin for [mngr](https://github.com/imbue-ai/mngr) that injects mngr configuration and dependencies into remote hosts during provisioning. This allows agents running on remote machines to use mngr themselves.
